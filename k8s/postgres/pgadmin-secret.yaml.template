# pgAdmin Credentials Secret Template
# DO NOT commit this file with actual credentials!
# Copy to pgadmin-secret.yaml and update values

apiVersion: v1
kind: Secret
metadata:
  name: pgadmin-credentials
  namespace: infra
type: Opaque
stringData:
  email: "admin@yourdomain.com"        # Change this
  password: "CHANGE-ME-STRONG-PASSWORD" # Generate with: openssl rand -base64 24
